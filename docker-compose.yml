version: '3.8'

services:
  frontend:
    build: .
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
    # Note: This frontend expects a 'backend' service to be available on the same network
    # or reachable via the name 'backend'.
